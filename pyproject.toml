[project]
name = "ecommerce-analytics-dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-duckdb>=1.10.1",
]

[dependency-groups]
dev = [
    "shandy-sqlfmt>=0.30.0",
]
